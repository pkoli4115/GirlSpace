package com.girlspace.app.data.chat

/**
 * Legacy compatibility file.
 *
 * We now use ChatMessage + ChatThread from their own files.
 * This file keeps aliases only, so there are NO duplicate data classes.
 */

typealias ChatMessageModel = ChatMessage
typealias ChatThreadModel = ChatThread
